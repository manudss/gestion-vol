#include "../structure.h"

ptr_t_temps quelheure(ptr_t_temps temps)
{
    t_temps tmp;
    int difjour;
    double delta;
    int depass=0;

    tmp.deb = time(NULL) ;




    delta = difftime(tmp.deb,temps->deb);




    if (depass = 1)          // Si on a changer d'heure
    {
       /* do
        {

            effacementjour ( );    // effacement des jours dans l'arbre avl
            effacementcolsclient( ); // effacement dans la liste des vols en cours du client
            difjour --;
        while ( difjour != 0)
        enregistrement ( );    // enregistrement dans les fichiers
        */
    }
}




